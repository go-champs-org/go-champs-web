it('renders without crashing', () => {
  const div = document.createElement('div');
  // TODO (lairjr): Need to adjust react-dnd dependency in unit test.
  // ReactDOM.render(<App />, div);
  // ReactDOM.unmountComponentAtNode(div);
});
